import { MaterialBlockModule } from './material-block-module';

describe('MaterialBlockModule', () => {
  it('should create an instance', () => {
    expect(new MaterialBlockModule()).toBeTruthy();
  });
});
